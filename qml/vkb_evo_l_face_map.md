# VKBsim Gladiator EVO L face lock

Source pack: `qml/maps/vkb_evo_r.json` mirrored to `qml/maps/vkb_evo_l.json`.
Photo: `qml/images/vkb_gladiator_evo_l.jpg` (horizontal flip of `vkb_gladiator_rig.jpg`), also packed as `qml/maps/vkb_evo_l/photo.jpg`.

Hardware `nx` is `1 - EVO_R.nx` on the same JPEG pixel grid. `ny` unchanged.
Page `chipFx` / `hotFx` / `fx` and leader `pin` left/right are mirrored with the photo.
Member `ox` / `oy` and 5-way roles are not flipped.

Slug: any device name containing Gladiator plus `EVO L` or `OT L` → `vkb_evo_l`.
Do not measure window screenshots. Measure the flipped JPEG only.
