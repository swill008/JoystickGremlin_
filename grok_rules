Standing rules — Joystick Gremlin

Source of truth for Grok on this repo. Recall this file when asked.
Any change to rules is written here. Do not keep a second unofficial list.

1. Do not move large files. Edit files using copy/replace/paste and patch operations. Keep the structure of the program. Warn the user if a change could break a previous version.
2. Any patches generated or cleanup necessary Grok is required to fix/patch themself. Do not ask the user unless Grok is unable to do so.
3. Plan first. No code until the user gives explicit confirmation.
   3.1 Confirmation is one of: go, ok go, code it, implement, land it, push it.
   3.2 Thoughts, analysis, options, “what would you do”, and screenshots are not confirmation. Answer those with a plan only.
   3.3 Do not start a patch, PUT, or commit until that confirmation is in the same turn or the immediately preceding turn for that exact change.
4. Announce every git push.
5. Branch: only Xbox_integration_ViGEmClient_dll for Xbox work. Optimization line stays on R15-OSC-Optimization. Do not mix Xbox commits onto Optimization unless you say merge.
6. Do not touch feature/osc-streamdeck unless you say so.
7. Stay on JoystickGremlin_, not JoystickGremlinEx_.
8. Warn before high CPU / memory / perf changes. Suggest cheaper options first.
9. You pull/fetch locally (GitHub Desktop). All pushes use the user's GitHub PAT against the Contents API (PUT), any file, any size. Do not use MCP create_or_update_file or push_files. You run with:
   poetry run python joystick_gremlin.py
   from E:\Users\Stacie\Documents\GitHub\JoystickGremlin_
10. Merge to “the program” only after you test.
11. Portable zip only, not a Program Files installer.
12. Do not reintroduce custom dill.dll loaders.
13. Do not commit ViGEmClient.dll to the repo from here (you already pushed it; that was your call).
14. Show generated / planned images before wiring new faces into QML, unless you already said go.
15. Xbox tab is labeled layout + incoming sources, not a DILL input device. Hardware maps with Map to Xbox.
16. Keep older R15 XML profiles loadable. Xbox-only nodes (map-to-xbox) are extra; stock R15 will not understand them.
17. Standing rules live in this file (grok_rules) at the program folder root. Recall this file. Any rule change is written here.
18. Images shown to the user
    18.1 Display every generated or planned image in chat with in-chat image generation / image edit (the method that attached the Option A plus-cluster overlay). Never use render_file or a file-preview card for pictures — that showed raw XML and the user saw nothing.
    18.2 Overlay mockups on the real source photo only: qml/images/vkb_gladiator_rig.jpg or a live Button Map shot the user sent. Do not invent a different controller.
    18.3 One image at a time. Ask saw it / blank before the next. If blank, one search-image test; if still blank, stop generating and implement.
    18.4 Do not rely on a file written to disk (imagine_images/) as proof the user saw it. The chat image is the proof.
19. GitHub uploads — PAT, no size gate. Until the user says otherwise:
    19.1 Use the user's GitHub PAT for every push. Contents API PUT of the full file. Any size. One or many files. Do not ask for a new token. Do not wait on the GitHub MCP file tools.
    19.2 Skip the 20k size check. Skip MCP create_or_update_file and push_files. Those truncated files and took too long.
    19.3 Method: Python requests, Authorization Bearer <PAT>, PUT https://api.github.com/repos/swill008/JoystickGremlin_/contents/<path> with message, content (base64), sha, branch. Branch for Button Map is VKB-joystick-Button-Map.
    19.4 Never write the PAT into grok_rules, the repo, commit messages, or artifacts. It lives in project instructions / this chat only.
    19.5 After a PUT, confirm the committed size matches the local file and the file is not PLACEHOLDER / empty. For device.py also confirm class Device, InputIdentifier, map-to-vjoy (and map-to-xbox when that token is expected). Last known good device.py: e592376, 60033 bytes.
    19.6 This PAT rule stands until the user says otherwise.
20. EVO R hardware lock and measured JPEG inventory live in qml/vkb_evo_r_face_map.md. Read that file before changing hardware ids or measured JPEG centroids. Chip / hotspot / spine placement at runtime is qml/maps/vkb_evo_r.json. When a measured centroid changes, update the md row and the JSON node nx/ny in the same commit.
21. Face leaders — precision only. No guessing. No window-screenshot math.
    21.1 Root cause of missed leaders: (a) nx/ny were read off a GUI screenshot that includes gutters and chrome, but px/py are fractions of the painted JPEG only; (b) tag ends used mapToItem(_face) while photo ends used _stage.x, which is not face space. Do not repeat either.
    21.2 Measure on the source bitmap only. For EVO R that is qml/images/vkb_gladiator_rig.jpg (899 x 920). Pixel x,y of the control, then nx = x / width, ny = y / height.
    21.3 Prove the dots on a copy of that JPEG and show it in chat before changing QML. The user confirms the overlay, not a window screenshot.
    21.4 Write the confirmed row into qml/vkb_evo_r_face_map.md and the matching node nx/ny in qml/maps/vkb_evo_r.json in the same commit.
    21.5 Both ends of every leader must use the same item space:
         photoPt(nx, ny) = _img.mapToItem(_face, _ox + nx * _pw, _oy + ny * _ph)
         tagPt = item.mapToItem(_face, …)
         Never use _stage.x / _stage.y as face coordinates.
    21.6 If a dot is still off, change one pixel pair on the JPEG overlay, then one map row. Do not nudge from a window screenshot.
22. Button Map work lives on VKB-joystick-Button-Map (cut from Xbox_integration_ViGEmClient_dll). Do not mix Button Map commits onto R15-OSC-Optimization unless you say merge.
23. Button Map dest is Mapped* vjoyLabel Repeater on the face. Callouts are physical EVO R marks only.
24. EVO R hardware lock (device name VKBsim Gladiator EVO R). Dual photo is the same grip in two poses.
    1/2 trigger
    3 red
    4 cap
    5 lower white
    6-10 5-way
    11-15 top-right 5-way
    16-20 silver wheel
    21/22 paddle
    23/24 En2
    25/26 En1
    27 mid pad
    28 left pad
    29 right pad
    hat1 8-way ministick
    axes 1-3 gimbal X/Y/Z
    axis 4 Z slider
25. Button Map editor: Edit on the map window. Save writes control.hardware JSON to qml/maps/<device>.json (EVO R: qml/maps/vkb_evo_r.json). That JSON is live runtime truth for chips, hotspots, spines, and image. Cancel drops the session. qml/vkb_evo_r_face_map.md is the measured JPEG inventory and hardware lock, not live layout.
26. No hacks. Follow Qt / QML / Gremlin best practice. If a fix needs a workaround (height: 0 to hide menu rows, hover-open by hand, screenshot math, dummy objects), stop and warn before coding. Propose the supported API first. Only ship a workaround if the user accepts it after that warning.
